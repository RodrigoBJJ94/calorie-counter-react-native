import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    imageFood: {
        position: 'absolute',
    },
    containerImageFood: {
        alignItems: 'center',
        bottom: -234,
    }
});
