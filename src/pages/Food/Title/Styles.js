import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    titleFood: {
        fontFamily: 'Montserrat-Regular',
        fontSize: 16.5,
        padding: 20,
        textAlign: 'center',
        marginTop: -10,
        color: '#fff',
    }
});
