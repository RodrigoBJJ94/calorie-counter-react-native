import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    resultFood: {
        fontFamily: 'Montserrat-Bold',
        fontSize: 20,
        marginTop: 20,
        color: '#fff',
    }
});
