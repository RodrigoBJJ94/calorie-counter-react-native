import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    titleMenu: {
        fontFamily: 'Montserrat-ExtraBold',
        fontSize: 50,
        marginTop: -60,
        marginBottom: 20,
        color: '#fff',
    }
});
