import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    containerImageDrink: {
        alignItems: 'center',
        bottom: -150,
    },
    imageDrink: {
        position: 'absolute',
    }
});
