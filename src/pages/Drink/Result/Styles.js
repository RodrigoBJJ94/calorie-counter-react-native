import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    resultDrink: {
        fontFamily: 'Montserrat-Bold',
        fontSize: 20,
        marginTop: 20,
        color: '#fff',
    },
});
